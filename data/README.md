# Local test photos

Put your own photos in `data/input/`, then run:

```powershell
.\test_model.ps1
```

Annotated results are written to:

```text
runs/predict/electrocom61-test/
```

Both `data/input/` and `data/output/` are ignored by Git.
